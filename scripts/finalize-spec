#!/usr/bin/env bash
set -euo pipefail

if [[ ! -x .venv/bin/python ]]; then
  echo "BLOCKED: .venv/bin/python is required; bootstrap the workspace first." >&2
  exit 1
fi

exec .venv/bin/python scripts/finalize_spec.py "$@"
