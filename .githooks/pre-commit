#!/bin/sh
set -eu
branch=$(git branch --show-current)
if [ -z "$branch" ]; then
  echo "Refusing commit from detached HEAD." >&2
  exit 1
fi
if [ "$branch" = "main" ]; then
  echo "Refusing direct commit on main; use a dedicated working branch." >&2
  exit 1
fi
